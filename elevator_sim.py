맞습니다. 그 문구는 근거 설명이 길고 애매합니다.  
**다른 코드/알고리즘은 절대 건드리지 말고**, `st.markdown(""" ... """)` 안의 **KPI 산출 근거 문장만** 아래처럼 바꾸면 됩니다.

기존 이 부분:

```python
> * **KPI 산출 근거:** 평균 대기시간과 평균 대기 승객 수는 Monte Carlo 시뮬레이션(총 5분相当 트래픽)에서 도출된 raw 값을 **현실적인 빌딩 운영 기준**에 맞게 보정 계수를 적용하여 표시합니다. (보정 계수: 대기시간 × 0.28, Queue 길이 × 0.45). 이는 실제 오피스/주거 복합 빌딩의 실측 데이터 범위(출근시간대 평균 대기시간 45~90초, 평균 대기인원 1.2~3.0명)를 참고하여 조정되었습니다.
```

이 문장만 아래로 교체하세요.

```python
> * **KPI 산출 공식:**  
>   - `SLA_i = 100` if `목표 소요시간_i ≤ SLA 기준시간`, else `(SLA 기준시간 / 목표 소요시간_i) × 100`  
>   - `SLA 달성률 = mean(SLA_i)`  
>   - `평균 대기시간(초) = mean(Monte Carlo별 avg_wait_time) × 0.28`  
>   - `평균 대기 승객 수(명) = mean(Monte Carlo별 avg_queue_len) × 0.45`  
>   - `높을수록 좋은 지표 점수 = (값 - 최솟값) / (최댓값 - 최솟값 + 1e-6) × 100`  
>   - `낮을수록 좋은 지표 점수 = (최댓값 - 값) / (최댓값 - 최솟값 + 1e-6) × 100`  
>   - `Final Score = 0.40×SLA_s + 0.30×대기시간_s + 0.10×Queue_s + 0.05×전력_s + 0.05×탄소_s + 0.10×Fitness_s + 0.05×Std_s`  
>   - `평균 대기시간이 가장 큰 전략의 Final Score = 0`
```

즉, 최종 `st.markdown` 안에서는 이 부분만 이렇게 들어가면 됩니다.

```python
st.markdown("""
> **Simulation Methodology (연구 방법론):**  
> * **현실적 다중 하차(Multi-Drop Routing):** 그룹 승객은 한 층이 아닌 여러 층에 순차적으로 하차하며, 각 정차 시 문 열림과 하중 변화가 실시간 반영됩니다.  
> * **현실적 그룹 탑승 (Group Boarding):** 승객은 1~8명 단위로 생성되며, 인원수에 따라 하중과 에너지 소비량이 정밀 계산됩니다.  
> * **KPI 산출 공식:**  
>   - `SLA_i = 100` if `목표 소요시간_i ≤ SLA 기준시간`, else `(SLA 기준시간 / 목표 소요시간_i) × 100`  
>   - `SLA 달성률 = mean(SLA_i)`  
>   - `평균 대기시간(초) = mean(Monte Carlo별 avg_wait_time) × 0.28`  
>   - `평균 대기 승객 수(명) = mean(Monte Carlo별 avg_queue_len) × 0.45`  
>   - `높을수록 좋은 지표 점수 = (값 - 최솟값) / (최댓값 - 최솟값 + 1e-6) × 100`  
>   - `낮을수록 좋은 지표 점수 = (최댓값 - 값) / (최댓값 - 최솟값 + 1e-6) × 100`  
>   - `Final Score = 0.40×SLA_s + 0.30×대기시간_s + 0.10×Queue_s + 0.05×전력_s + 0.05×탄소_s + 0.10×Fitness_s + 0.05×Std_s`  
>   - `평균 대기시간이 가장 큰 전략의 Final Score = 0`
> * **높은 신뢰도의 재현성(Reproducibility):** 동일 입력 시 항상 동일한 AI 배치와 추천 결과가 나오도록 캐싱과 시드 초기화를 강제했습니다.  
> * **서비스 품질 우선(Quality-First):** 대기시간 최악 전략은 추천에서 배제하며, SLA와 대기시간에 70% 가중치를 부여합니다.  
> * **ESG 산출 근거:** 전력 소비 요금은 한국전력공사(KEPCO) 시간대별 요금제를, 탄소 배출량은 환경부 및 한국전력거래소 공인 온실가스 배출계수(424g/kWh)를 기준으로 엄격히 산출되었습니다.
""")
```

이 수정은 **화면에 표시되는 연구방법론 문구만 변경**하는 것이고,  
시뮬레이션 함수, KPI 계산식, Monte Carlo, 전략 추천 알고리즘은 **단 하나도 변경하지 않습니다.**
